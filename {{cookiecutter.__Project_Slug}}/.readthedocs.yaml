version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "{{ cookiecutter.python_ver }}"
  jobs:
    pre_install:
      - pip install -r docs/requirements.txt

python:
  install:
    - method: pip
      path: .

sphinx:
  builder: html
  configuration: docs/conf.py
